== Future Work

Two upstream infrastructure blockers gate further command migration merges. Both sit ahead of me in the mentor's queue.

1. *Idempotency infrastructure.* Needed to guarantee at-most-once semantics for command handlers. A pre-GSoC attempt stalled.
2. *Maker-checker.* Proposal on the dev list, then implementation. The new framework must preserve existing maker-checker semantics for endpoints that had them.

Once those land, remaining modules (Loans, Savings, Charges, ...) can follow the same migration recipe.

.CI note
[NOTE]
====
The `WorkingCapitalDiscountFeeAmortizationAdjustment` E2E feature fails on some shards. Verified locally that the failure reproduces on stock `upstream/develop` — not caused by my PRs.
====
