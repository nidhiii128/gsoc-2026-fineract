== Fineract Architecture at a Glance

Where the command layer sits in the overall stack.

image::architecture-stack.svg[Fineract stack, 500]

The command layer is the plumbing that turns REST requests into service calls. It's what runs the audit trail, permission checks, retry logic, and JSON deserialization — every write endpoint passes through it.
